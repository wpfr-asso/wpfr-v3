jQuery(document).ready( function() {

            
      var cssObj = {
        position: "fixed",
        top: offset.top,
        left: offset.left
      }
      //jQuery('#col_right_comment').css(cssObj);
});